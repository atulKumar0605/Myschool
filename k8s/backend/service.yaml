apiVersion: v1
kind: Service
metadata:
  name: myschool-backend
  namespace: myschool-backend
spec:
  type: ClusterIP

  selector:
    app: myschool-backend

  ports:
    - name: http
      port: 8080
      targetPort: 8080
